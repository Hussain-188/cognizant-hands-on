# Exercise 4 — Creating and Configuring a Maven Project

This is the `LibraryManagement` Maven project for the Spring Core exercises.

Quick commands:

```bash
mvn clean package
```

Notes:

- Includes Spring dependencies: `spring-context`, `spring-aop`, `spring-webmvc`.
- Compiler is configured for Java 1.8 via `maven-compiler-plugin`.
